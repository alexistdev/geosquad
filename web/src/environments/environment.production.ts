export const environment = {
  production: true,
  // Diisi saat deploy kalau API dilayani dari origin berbeda. Kalau API
  // berada di belakang reverse proxy yang sama, biarkan path relatif.
  apiUrl: '/api/v1',
};
